% Fruit and Colour Database

fruit(apple, red).
fruit(banana, yellow).
fruit(orange, orange).
fruit(grapes, green).
fruit(mango, yellow).
fruit(guava, green).

% Query predicate
show_fruit_colour :-
    fruit(Fruit, Colour),
    write(Fruit),
    write(' is '),
    write(Colour),
    nl,
    fail.

show_fruit_colour.